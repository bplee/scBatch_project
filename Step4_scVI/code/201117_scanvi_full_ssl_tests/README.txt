This directory contains the results of 6 full SSL trained scANVI models

No models were saved however, this is something to work on, but the scVI API is pretty 
    simple to use, so not a huge issue

One model takes a few hours to run, its not horrible.
