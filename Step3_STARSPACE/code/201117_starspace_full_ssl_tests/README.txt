This folder contains 6 STARSPACE full SSL models.

The model files and `.tsv` files are in `./starspace_models`.
    To load a previous model we want to look in this dir

The `./train_files` dir contains the files that were used to train each of the models.
